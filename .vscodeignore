.vscode/**
.tic-code/**
node_modules/**
src/**
mockup.html
*.vsix
tsconfig.json
package-lock.json
reversa-main/**
# resources/reversa/ is intentionally NOT ignored — it is bundled in the extension package
# so that the Reversa Engine assets (agents/templates/docs) are available at runtime.
